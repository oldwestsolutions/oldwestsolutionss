'use client';

import { Button } from '@fluentui/react-components';

export default function TestPage() {
  return (
    <div style={{ padding: 40 }}>
      <Button appearance="primary">Test Fluent UI Button</Button>
    </div>
  );
} 